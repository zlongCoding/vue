import Loading from './Loading.vue'
import navigation from './navigation.vue'
import dialogbg from './dialogbg.vue'
export default(Vue) => {
  Vue.component('loading', Loading)
  Vue.component('navigation', navigation)
  Vue.component('dialogbg', dialogbg)
}
