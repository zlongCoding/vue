// import $ from '@/utils'

export default {
  PROCESS_SUBMIT: ({commit, state}, data) => {
    // $.post('/auto/policy/submit', data).then(json => json, json => {
    //   window.Mint.Indicator.close()
    // })
  }
}
