export default {
  services_company: (state, config) => {
    state.services.company = config
  },
  services_detail: (state, config) => {
    state.services.detail = config
  },
  services_hasbought: (state, config) => {
    state.services.hasBought = config
  },
  services_buyarea: (state, config) => {
    state.services.buyArea = config
  },
  services_vouchersno: (state, config) => {
    state.services.vouchersNo = config
  },
  services_maplist: (state, config) => {
    state.services.mapList = config
  }
}
